import React from "react";
import Button from "./button";

export default {
  title: "Components/Button",
  component: Button,
};

const Template = (args) => <Button url="#" title="Button" />;

export const ButtonStory = Template.bind({});
